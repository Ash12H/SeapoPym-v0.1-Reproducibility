# OS
.DS_Store
Thumbs.db

# Data files (heavy global forcing + raw intermediates are regenerated from CMEMS + scripts).
# Small station inputs under data/ are force-added (git add -f) and tracked despite *.zarr below.
*.zarr
*.nc
*.parquet
data/cache/

# Heavy intermediates: per-seed logbooks, raw Sobol tree, global sim outputs (regenerated, not committed)
/results_raw/

# figures/ holds the produced manuscript figures (PDF + PNG) and IS tracked (the deposit shows them).

# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# uv / venv
.venv/
.python-version

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# Logs
*.log
*.done

# Local review fork of the framework (a separate git repo, cloned alongside;
# referenced by pyproject's [tool.uv.sources] while the PR is un-merged)
dependencies/
tmp/